{
  "scripts": {
    "build": "tsc",
    "dev": "vercel dev",
    "start": "vercel start",
    "clean": "rm -rf dist/ node_modules/ .vercel/",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@vercel/node": "^5.1.16",
    "axios": "^1.9.0"
  },
  "devDependencies": {
    "@types/axios": "^0.9.36"
  }
}
